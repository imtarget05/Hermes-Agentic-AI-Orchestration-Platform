# Hermes on HF Spaces — dependencies (no `pip install .`; src/ is on sys.path in app.py)
gradio>=5.0
fastapi>=0.115
uvicorn>=0.30
pydantic>=2.0
pydantic-settings>=2.0
python-dotenv>=1.0
click>=8.1
httpx>=0.27
psycopg[binary]>=3.1
